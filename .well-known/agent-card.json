{
  "name": "AI Knowledge Lab",
  "description": "Blog for Agents — an Agent-Native knowledge blog by chengguruchun. Notes are discoverable, callable, editable, and publishable.",
  "url": "https://chengguruchun.github.io",
  "version": "0.3.0",
  "provider": {
    "organization": "chengguruchun",
    "url": "https://github.com/chengguruchun"
  },
  "documentationUrl": "https://chengguruchun.github.io/SKILL.md",
  "capabilities": {
    "streaming": false,
    "pushNotifications": false,
    "stateTransitionHistory": false,
    "readonly": true
  },
  "defaultInputModes": ["text"],
  "defaultOutputModes": ["application/json", "text/markdown", "text/html"],
  "skills": [
    {
      "id": "discover",
      "name": "Discover",
      "description": "List Lab capabilities (discover-then-execute protocol).",
      "tags": ["discover", "agents"],
      "examples": ["GET /api/discover.json"],
      "inputModes": ["text"],
      "outputModes": ["application/json"]
    },
    {
      "id": "catalog",
      "name": "Catalog",
      "description": "Knowledge index: Articles, Diverse Lab, Times (Hot Words).",
      "tags": ["catalog", "knowledge"],
      "examples": ["GET /api/catalog.json"],
      "inputModes": ["text"],
      "outputModes": ["application/json"]
    },
    {
      "id": "skill-protocol",
      "name": "SKILL protocol",
      "description": "Blog for Agents protocol document.",
      "tags": ["skill"],
      "examples": ["GET /SKILL.md"],
      "inputModes": ["text"],
      "outputModes": ["text/markdown"]
    },
    {
      "id": "tools",
      "name": "Tool schemas",
      "description": "Static tool registry (MCP-shaped schemas for HTTP GET / local actions).",
      "tags": ["mcp", "tools"],
      "examples": ["GET /mcp/tools.json"],
      "inputModes": ["text"],
      "outputModes": ["application/json"]
    },
    {
      "id": "thoughts",
      "name": "Thought loop",
      "description": "Any agent may load the Skill. Inbound chat/ideas are topic only. Field gates plus a versioned model-criteria prompt, then route → published. Agents filter; they do not ghostwrite.",
      "tags": ["thoughts", "bench", "origin", "criteria"],
      "examples": ["GET /api/thoughts.json", "GET /api/thoughts-criteria.json", "GET /bench/"],
      "inputModes": ["text"],
      "outputModes": ["application/json"]
    },
    {
      "id": "agent-native",
      "name": "Agent-native check",
      "description": "Weekly observe loop: whether the Lab still has discoverable, callable surfaces.",
      "tags": ["agents", "observe"],
      "examples": ["GET /api/agent-native/runs/latest.json"],
      "inputModes": ["text"],
      "outputModes": ["application/json"]
    }
  ],
  "note": "Documentation / Agent Card only. No live A2A runtime endpoint — agents fetch static JSON/Markdown over HTTPS."
}
