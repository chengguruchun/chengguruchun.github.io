name: Rebuild feeds and SEO

on:
  push:
    paths:
      - "content/**"
      - "api/catalog.json"
      - "api/articles.json"
      - "scripts/build_feeds.py"
      - "scripts/build_seo.py"
      - "**/*.html"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  feeds:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Build feed.xml and api/feeds.json
        run: python3 scripts/build_feeds.py
      - name: Rebuild page metadata, sitemap and robots
        run: python3 scripts/build_seo.py
      - name: Commit if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add feed.xml api/feeds.json sitemap.xml robots.txt "*.html"
          if git diff --staged --quiet; then
            echo "No feed or SEO changes"
          else
            git commit -m "chore: regenerate RSS feed and page metadata"
            git push
          fi
